spec: 64
name: "LP-Frost Mage Spellslinger"
author: "LPFrost - SS"
version: "0.6.27"
description: "Its cold in here"

config:
  use_cooldowns:
    label: "Use major cooldowns (Time Warp, trinkets, racials)"
    section: "Cooldowns"
    type: checkbox
    default: true
  cds_group_min_ttd:
    label: "Major CDs — min real group TTD (sec; 0 = no gate; Simia default TTD 9999 = unset, ignored)"
    section: "Cooldowns"
    type: slider
    default: 25
    min: 0
    max: 120
  use_defensives:
    label: "Auto-cast Ice Barrier / Ice Block"
    section: "Defensives"
    type: checkbox
    default: true
  auto_interrupt:
    label: "Auto interrupts (Counterspell / Fire Breath CC — uses shared Interrupts + interrupt.*.check)"
    section: "Interrupts"
    type: checkbox
    default: true
  auto_spellsteal:
    label: "Auto Spell Steal (purgable magic, enemies only)"
    section: "Utility"
    type: checkbox
    default: true
  auto_remove_curse:
    label: "Auto Remove Curse (self priority + party scan)"
    section: "Utility"
    type: checkbox
    default: true
  auto_arcane_intellect:
    label: "Auto Arcane Intellect — self + party (out of combat)"
    section: "Utility"
    type: checkbox
    default: true
  aoe_threshold:
    label: "AoE priority cutover (>= N enemies around target)"
    section: "Rotation"
    type: slider
    default: 3
    min: 2
    max: 8
  barrier_hp:
    label: "Ice Barrier refresh HP (%)"
    section: "Defensives"
    type: slider
    default: 95
    min: 50
    max: 100
  ice_block_hp:
    label: "Ice Block emergency HP (%)"
    section: "Defensives"
    type: slider
    default: 20
    min: 5
    max: 40

variables:
  is_aoe: enemies_around_target>=config.aoe_threshold
  flurry_ok: buff.brain_freeze.react&buff.thermal_void.down
  hardcast_safe_gs: target.time_to_die=0|target.time_to_die>action.glacial_spike.execute_time
  hardcast_safe_rof: target.time_to_die>action.ray_of_frost.execute_time
  orb_ready: (!player.moving&!player.channeling)
  # TTD defaults to 9999 until Simia has a real estimate — do not treat 9999 as "long enough".
  cds_group_ok: >
    config.cds_group_min_ttd=0|
    (group.lowest.time_to_die>0&group.lowest.time_to_die>=config.cds_group_min_ttd)
  # Instant Blizzard only — enough Freezing Rain left to stay instant; no hardcast line below.
  blizzard_fr_instant_ok: >
    buff.freezing_rain.up&buff.freezing_rain.remains>0.35
  # Blizzard only when more than 2 enemies around target (3+).
  blizzard_use_ok: >
    variable.blizzard_fr_instant_ok&enemies_around_target>2

lists:
  main:
    - ice_block,range_check=none,if=player.channeling&config.use_defensives&health.pct<config.ice_block_hp&cooldown.ice_block.ready
    - return,if=player.channeling
    - call_action_list,name=spell_queue
    - call_action_list,name=sanity_checks
    - call_action_list,name=auto_target_ranged
    - call_action_list,name=auto_heal
    - call_action_list,name=auto_mana
    - call_action_list,name=buffs,if=!player.combat
    - call_action_list,name=interrupts,if=config.auto_interrupt
    - call_action_list,name=auto_purge_magic,if=config.auto_spellsteal
    - call_action_list,name=dispel,if=config.auto_remove_curse
    - call_action_list,name=defensives,if=config.use_defensives
    - return,if=config.auto_combat.has(0)
    - return,if=config.auto_combat>=1&!auto_combat
    - return,if=target.dead|!target.attackable|!target.enemy
    - call_action_list,name=cooldowns,if=config.use_cooldowns
    - call_action_list,name=rotation
    - call_action_list,name=movement,if=player.moving

  buffs:
    - arcane_intellect,range_check=none,if=config.auto_arcane_intellect&(!buff.arcane_intellect.up|buff.arcane_intellect.remains<300)
    - arcane_intellect,delay=500,cycle=party,if=config.auto_arcane_intellect&(cycle.buff.arcane_intellect.down.any|cycle.buff.arcane_intellect.remains.any<300)
    - ice_barrier,range_check=none,if=!buff.ice_barrier.up&cooldown.ice_barrier.charges>=1

  interrupts:
    - counterspell,interrupt=true,if=interrupt.target.check
    - counterspell.mouseover,range_check=mouseover,interrupt=true,if=interrupt.mouseover.check
    - counterspell.focus,range_check=focus,interrupt=true,if=interrupt.focus.check
    - fire_breath,interrupt=true,if=interrupt.cc.check&!cooldown.counterspell.ready&target.range<=10

  auto_purge_magic:
    - return,if=!config.auto_spellsteal
    - spellsteal,if=target.enemy&target.purgeable.magic

  dispel:
    - remove_curse,delay=600,range_check=none,if=player.dispelable.curse
    - remove_curse,delay=1200,cycle=party,if=cycle.dispelable.list

  defensives:
    - ice_barrier,range_check=none,if=buff.ice_barrier.down&cooldown.ice_barrier.charges>=1&health.pct<config.barrier_hp
    - ice_block,range_check=none,if=health.pct<config.ice_block_hp&cooldown.ice_block.ready

  cooldowns: []

  aoe_opener:
    - flurry,if=variable.flurry_ok&lastcast.flurry>=10&ttd
    - frozen_orb,if=variable.orb_ready&state.cds&lastcast.frozen_orb>=10
    - blizzard,if=variable.blizzard_use_ok&lastcast.blizzard>=10
    - ray_of_frost,if=state.cds&variable.hardcast_safe_rof&lastcast.ray_of_frost>=10

  st_opener:
    - ray_of_frost,if=state.cds&variable.hardcast_safe_rof&lastcast.ray_of_frost>=10
    - ice_lance,if=lastcast.ice_lance>=10

  rotation:
    - call_action_list,name=aoe_opener,if=variable.is_aoe&combat.time<=10
    - call_action_list,name=st_opener,if=!variable.is_aoe&combat.time<=10
    - comet_storm,if=talent.comet_storm
    - blizzard,if=variable.blizzard_use_ok
    - flurry,if=variable.flurry_ok
    - frozen_orb,if=state.cds&variable.orb_ready
    - ice_lance,if=buff.fingers_of_frost.stack=2
    - glacial_spike,if=variable.hardcast_safe_gs
    - ice_lance,if=buff.fingers_of_frost.react
    - ice_lance,if=debuff.freezing.stack>=6
    - flurry,if=cooldown.flurry.ready
    - ray_of_frost,if=state.cds&variable.hardcast_safe_rof
    - frostbolt,if=!player.moving
    - ice_lance

  movement:
    - blizzard,if=variable.blizzard_use_ok
    - ice_lance
